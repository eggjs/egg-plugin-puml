'use strict';

const path = require('path');

module.exports = {
  'hyphen-name': {
    enable: true,
    path: path.join(__dirname, 'hyphen'),
  },
  'hyphen-dep': {
    enable: true,
    path: path.join(__dirname, 'hyphen-dep'),
  },
};
